@model _50_Ders_MVC_Projesi.Models.Entities.Category
@{
    ViewBag.Title = "Update";
    Layout = "~/Views/Shared/_mainLayout.cshtml";
}

<h5 class="text-center text-primary">Update<i class="fa-solid fa-edit mx-2"></i></h5>
<form method="post">
    <input type="hidden" value="@Model.KATEGORIID" name="KATEGORIID"/>
    <div>
        <label class="form-label">Kategori Adı</label>
        <input type="text" class="form-control" name="KATEGORIAD" value="@Model.KATEGORIAD" />
    </div>
    <div class="mt-2">
        <button class="btn btn-outline-warning my-3 "><i class="fa-solid fa-edit me-1"></i>Kategri Güncelle </button>
    </div>

</form>

