@model _50_Ders_MVC_Projesi.Models.Entities.Category
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_mainLayout.cshtml";
}

<h5 class="text-center text-primary"><i class="fas fa-plus me-1"></i>Create</h5>
<form method="post">
    <div>
        <label class="form-label">Kategori Adı</label>
        <input type="text" class="form-control" name="KATEGORIAD" />
        
          @Html.ValidationMessageFor(k=>k.KATEGORIAD, "" , new {@class="text-danger"})           
        
    </div>
    <div class="mt-2">
        <button class="btn btn-info my-3 text-white"><i class="fas fa-plus me-1"></i>Kategri Ekle</button>
    </div>

</form>

